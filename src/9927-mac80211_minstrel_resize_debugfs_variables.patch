Index: backports-5.4.27-1/net/mac80211/rc80211_minstrel_debugfs.c
===================================================================
--- backports-5.4.27-1.orig/net/mac80211/rc80211_minstrel_debugfs.c
+++ backports-5.4.27-1/net/mac80211/rc80211_minstrel_debugfs.c
@@ -95,15 +95,15 @@ minstrel_stats_open(struct inode *inode,
 
 		p += sprintf(p, "%4u.%1u    %4u.%1u     %3u.%1u"
 				"     %3u   %3u %-3u   "
-				"%9llu   %-9llu\n",
+				"%9u   %-9u\n",
 				tp_max / 10, tp_max % 10,
 				tp_avg / 10, tp_avg % 10,
 				eprob / 10, eprob % 10,
 				mrs->retry_count,
 				mrs->last_success,
 				mrs->last_attempts,
-				(unsigned long long)mrs->succ_hist,
-				(unsigned long long)mrs->att_hist);
+				mrs->succ_hist,
+				mrs->att_hist);
 	}
 	p += sprintf(p, "\nTotal packet count::    ideal %d      "
 			"lookaround %d\n\n",
@@ -152,14 +152,15 @@ minstrel_stats_csv_open(struct inode *in
 
 		p += sprintf(p, "%u.%u,%u.%u,%u.%u,%u,%u,%u,"
 				"%llu,%llu,%d,%d\n",
+				"%u,%u,%d,%d\n",
 				tp_max / 10, tp_max % 10,
 				tp_avg / 10, tp_avg % 10,
 				eprob / 10, eprob % 10,
 				mrs->retry_count,
 				mrs->last_success,
 				mrs->last_attempts,
-				(unsigned long long)mrs->succ_hist,
-				(unsigned long long)mrs->att_hist,
+				mrs->succ_hist,
+				mrs->att_hist,
 				mi->total_packets - mi->sample_packets,
 				mi->sample_packets);
 
Index: backports-5.4.27-1/net/mac80211/rc80211_minstrel_ht_debugfs.c
===================================================================
--- backports-5.4.27-1.orig/net/mac80211/rc80211_minstrel_ht_debugfs.c
+++ backports-5.4.27-1/net/mac80211/rc80211_minstrel_ht_debugfs.c
@@ -103,15 +103,15 @@ minstrel_ht_stats_dump(struct minstrel_h
 
 		p += sprintf(p, "%4u.%1u    %4u.%1u     %3u.%1u"
 				"     %3u   %3u %-3u   "
-				"%9llu   %-9llu\n",
+				"%9u   %-9u\n",
 				tp_max / 10, tp_max % 10,
 				tp_avg / 10, tp_avg % 10,
 				eprob / 10, eprob % 10,
 				mrs->retry_count,
 				mrs->last_success,
 				mrs->last_attempts,
-				(unsigned long long)mrs->succ_hist,
-				(unsigned long long)mrs->att_hist);
+				mrs->succ_hist,
+				mrs->att_hist);
 	}
 
 	return p;
@@ -247,15 +247,15 @@ minstrel_ht_stats_csv_dump(struct minstr
 		eprob = MINSTREL_TRUNC(mrs->prob_avg * 1000);
 
 		p += sprintf(p, "%u.%u,%u.%u,%u.%u,%u,%u,"
-				"%u,%llu,%llu,",
+				"%u,%u,%u,",
 				tp_max / 10, tp_max % 10,
 				tp_avg / 10, tp_avg % 10,
 				eprob / 10, eprob % 10,
 				mrs->retry_count,
 				mrs->last_success,
 				mrs->last_attempts,
-				(unsigned long long)mrs->succ_hist,
-				(unsigned long long)mrs->att_hist);
+				mrs->succ_hist,
+				mrs->att_hist);
 		p += sprintf(p, "%d,%d,%d.%d\n",
 				max(0, (int) mi->total_packets -
 				(int) mi->sample_packets),
