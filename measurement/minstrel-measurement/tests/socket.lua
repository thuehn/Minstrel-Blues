
--    local socket = require("socket")
--    local udp = assert(socket.udp())
--    assert(udp:sendto("anything", ip, port))
--    -- port 13 daytime protocoll
--    io.write(assert(udp:receive()))

